# Rust Integration Tests

This directory is reserved for future Rust integration tests.

Currently, unit tests are co-located with source files using `#[cfg(test)]` modules.
Integration tests will be added here as needed for testing cross-module interactions
and Tauri IPC command handlers.

